import { appFeatureKey , State as AppState, reducer as appReducer} from './app.reducers';

import * as fromActions from './app.actions';

import * as fromSelectors from './app.selectors';

export {fromActions, fromSelectors, appFeatureKey,AppState, appReducer}
