import * as fromActions from './post.action';
import * as fromSelectors from './post.selectors';
import {State as PostState, reducer as postReducer ,postFeatureKey} from './post.reducer';


export { fromActions, fromSelectors, PostState, postFeatureKey, postReducer };
